# Base URL of the inventory API. No trailing slash.
# Deployed backend:
VITE_API_URL=https://airline-inventory-engine.onrender.com
# For faster iteration against a local backend instead:
# VITE_API_URL=http://localhost:8000
